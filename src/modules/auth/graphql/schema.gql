scalar Date
scalar DateTime

type Query {
    aboutMe: AboutMe
}

type Mutation {
    doLogin(input: LoginInput!): String
}

type AboutMe {
    
}

input LoginInput {
    firebaseUUID: String!
    email: String!
}